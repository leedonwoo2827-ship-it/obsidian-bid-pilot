# bid-intelligence 플러그인 설정 매뉴얼

> 🎯 경로 설정(v1.1) + AI 채팅·RAG·멀티미디어·Apply Edit·MCP(v1.2) 통합 매뉴얼
>
> 📍 같은 매뉴얼이 실사용자 볼트에도 배치되어 있음:
> `C:\Users\leedonwoo\Documents\Obsidian Vault\bid-pilot\00_플러그인_설정매뉴얼.md`

## 배경 — 왜 이 설정이 필요한가

bid-intelligence 플러그인은 v1에서 `_context/` 폴더를 **볼트 루트에서만** 찾았습니다. v1.1부터 **설정에서 임의의 하위 폴더로 지정 가능** — 한 볼트에서 여러 프로젝트를 관리할 수 있습니다.

```
Obsidian Vault/                   ← 볼트 루트
├── bid-pilot/                    ← 유비온 수주 전용
│   ├── _context/                 ← 여기를 플러그인이 보도록 설정
│   └── _analysis/
├── koica-2026/                   ← (가상) 별도 사업
│   ├── _context/
│   └── _analysis/
└── ...
```

## 설정 단계 (3분)

### 1. 설정 화면 열기

- **⚙️ 왼쪽 하단 톱니바퀴** 클릭
- 또는 `Ctrl+,` 단축키

### 2. "Bid Intelligence" 탭 클릭

- 왼쪽 메뉴 **커뮤니티 플러그인** 섹션 아래 → **Bid Intelligence**

### 3. 최상단 "📁 프로젝트 경로" 섹션

두 입력창을 다음과 같이 채우고 "다시 그리기" 클릭:

| 필드 | 유비온 기본값 | 용도 |
|---|---|---|
| 컨텍스트 폴더 경로 | `bid-pilot/_context` | 회사 자료 저장 위치 |
| 분석 결과 폴더 경로 | `bid-pilot/_analysis` | `/bid-analyze`, `/brief` 결과 저장 위치 |

→ 버튼 클릭 즉시 사이드바의 **회사 컨텍스트** 패널이 실제 파일 수를 반영.

## 입력 규칙

### ✅ 허용
- 볼트 내 상대 경로: `_context`, `bid-pilot/_context`, `sub/deep/path/_context`
- 자동 정규화: 앞뒤 `/` 제거, `\` → `/` 변환

### ❌ 불가
- 절대 경로: `C:\...\_context`
- 볼트 외부 경로
- 빈 값 (자동으로 기본값 복원)

## 프로젝트 전환 치트시트

| 상황 | 컨텍스트 경로 | 분석 경로 |
|---|---|---|
| 유비온 기본 | `bid-pilot/_context` | `bid-pilot/_analysis` |
| 볼트 전체 수주 전용 | `_context` | `_analysis` |
| 신규 ODA 사업 | `koica-2026/_context` | `koica-2026/_analysis` |
| 신규 교육 SI | `edu-2026/_context` | `edu-2026/_analysis` |

## 명령어 사용법

플러그인은 **6개**의 명령(Command)을 제공합니다. `Ctrl+P` (명령 팔레트)에서 **`bid`** 입력 시 모두 노출됩니다.

### 단축키 설정 (선택)

자주 쓰는 명령은 단축키로 등록해두면 편리합니다.

1. **⚙️ 설정** → 왼쪽 메뉴 **단축키** 클릭
2. 우측 상단 검색창에 **`bid`** 입력 → 6개 명령이 필터링됨
3. 등록할 명령 옆 **`+` 아이콘** 클릭 → 원하는 키 조합 누르기
4. 충돌 경고가 뜨면 다른 조합으로 재시도

#### 추천 단축키 예시

| 명령 | 추천 단축키 | 사유 |
|---|---|---|
| 컨텍스트 매니저 열기 | `Ctrl+Shift+C` | 자료 현황 훑어볼 때 |
| 수주 AI 채팅 열기 | `Ctrl+Shift+K` | 가장 자주 사용 |
| 현재 파일 AI 분석 | `Ctrl+Shift+A` | 자료 추가 직후 |
| 컨텍스트 전체 분석 | (지정 안 함) | 호출량 많아 의도치 않은 실행 방지 |
| 컨텍스트 볼트 재인덱싱 (RAG) | (지정 안 함) | 드물게 실행 |

> ⚠️ **주의**: `Ctrl+Shift+C`는 OS·다른 플러그인에서 점유 중일 수 있습니다. 충돌 시 `Alt+`, `Ctrl+Alt+` 조합으로 대체.

### 명령 6종 상세

#### 1. 컨텍스트 매니저 열기 (좌측 사이드바)
- **하는 일**: 설정한 `_context/` 폴더의 모든 마크다운 파일을 카테고리·태그·요약별로 시각화하는 사이드 패널을 좌측에 엽니다.
- **사전 조건**: `_context/` 하위에 자료가 존재해야 함
- **언제 쓰나**: 수주 관련 자료(제안서·실적·인력)를 한 화면에서 훑고 싶을 때

#### 2. 수주 AI 채팅 열기 (우측 사이드바) ⚡ **Gemini API 키 필요 / v1.2**
- **하는 일**: 멀티턴 스트리밍 Gemini 채팅 UI를 우측 사이드바에 엽니다.
- **기능**: 핀한 노트 + RAG 자동 검색 + YouTube 자막 자동 첨부 + 그래프 캡처 + Apply Edit
- **언제 쓰나**: 제안서 초안 작성·문장 리라이팅·공고 적합도 토론 등 모든 AI 작업의 허브

#### 3. 현재 파일 AI 분석 (프로퍼티 생성) ⚡ **Gemini API 키 필요**
- **하는 일**: 현재 열려 있는 마크다운 파일 1개를 Gemini로 분석해 frontmatter(YAML 프로퍼티: `category`, `summary`, `tags`, `entities`, `relevance` 등)를 자동 생성·삽입합니다.
- **사전 조건**: 플러그인 설정에서 **Gemini API 키**가 입력되어 있어야 함
- **언제 쓰나**: 새로 추가한 자료 1건을 빠르게 분류·태깅하고 싶을 때

#### 4. 컨텍스트 전체 분석 ⚡ **Gemini API 키 필요 / 호출량 많음**
- **하는 일**: 설정된 `_context/` 폴더 안의 **모든 마크다운 파일을 순차적으로** Gemini로 분석합니다.
  - 이미 `summary` + `category` 프로퍼티가 있는 파일은 자동 스킵
  - 각 호출 사이 1.5초 대기 (API rate limit 방지)
- **결과**: 각 파일에 `type` / `category` / `summary` / `tags` / `entities` / `relevance` 프로퍼티 자동 채움.
- **주의**: 파일 수가 많으면 수 분 이상 소요. API 사용량(과금) 사전 확인 권장.

#### 5. 컨텍스트 볼트 재인덱싱 (RAG) ⚡ **v1.2**
- **하는 일**: 컨텍스트 폴더의 모든 마크다운을 `text-embedding-004`로 청크 임베딩하여 벡터 DB에 저장. 채팅에서 자동 근거 검색에 사용.
- **저장 위치**: `{vault}/.obsidian/plugins/bid-intelligence/vectors.json`
- **언제 쓰나**: 처음 설치 후 1회, 또는 벡터 DB가 손상됐다고 판단될 때. 평상시에는 파일 수정·추가 시 자동 증분 인덱싱되므로 수동 실행 불필요.

#### 6. 그래프를 채팅에 첨부하고 열기 ⚡ **v1.2**
- **하는 일**: 현재 활성 Obsidian 그래프 뷰의 canvas를 PNG로 캡처 → 채팅에 이미지 첨부 → 채팅 뷰 오픈.
- **사전 조건**: 그래프 또는 로컬 그래프 뷰가 이미 열려 있어야 함.
- **언제 쓰나**: "이 그래프에서 중심 노드는?", "자료 간 연결 밀도가 낮은 영역은?" 같은 시각적 분석을 AI에 요청할 때.

> ℹ️ **참고**: 이전 버전의 "브리핑 대시보드 열기"·"분석 리포트 뷰어 열기" 명령은 제거되었습니다. 뷰 클래스는 코드에 남아 있어 향후 부활 가능하나 현재는 명령 팔레트·리본에서 노출되지 않습니다.

### 권장 실행 순서 (처음 사용자)

1. **프로젝트 경로 설정** → 위의 "설정 단계" 참조
2. **Gemini API 키 입력** → Settings → 🤖 AI → API 키 + "테스트" 버튼
3. **컨텍스트 매니저 열기** → 현재 자료 현황 확인
4. **현재 파일 AI 분석** → 1개 파일로 동작 테스트
5. **컨텍스트 전체 분석** → frontmatter 일괄 생성
6. **컨텍스트 볼트 재인덱싱 (RAG)** → 채팅 근거 검색 준비
7. **수주 AI 채팅 열기** → 제안 작성 시작

---

## v1.2 — AI 채팅 사용법

### 기본 흐름

1. 우측 사이드바 **💬 수주 AI 채팅** 리본 또는 명령으로 채팅 오픈
2. 입력창 위 "📎 현재 노트" 버튼으로 현재 열린 노트를 **핀**
3. 질문 입력 → **Ctrl+Enter**로 전송
4. 스트리밍 응답 수신. 중단하고 싶으면 "중단" 버튼

### 컨텍스트 주입 방식

채팅은 **3단계로 컨텍스트를 자동 조립**해 Gemini에 전달합니다:

| 단계 | 대상 | 제어 |
|---|---|---|
| ① 핀 컨텍스트 | "📎 현재 노트" 등으로 사용자가 명시적으로 핀한 노트 | 핀 칩 클릭으로 해제 |
| ② RAG 자동 검색 | 질문과 의미가 가까운 `_context/` 청크 top-K | Settings → 🔍 RAG → 활성화/Top K |
| ③ 프리페치 | 메시지에 YouTube URL이 있으면 자막 자동 fetch | 자막 칩 × 버튼으로 제외 |

사용자 메시지 아래에 `참조: 📄 파일명, 🔍 _context/.../a.md, ▶️ 영상제목` 형태로 실제 사용된 컨텍스트가 표시됩니다.

### Apply Edit (AI 편집 제안 적용)

채팅에서 **"이 섹션을 더 간결하게 다시 써줘", "서론을 한 문단으로 줄여줘"** 같은 편집 요청을 하면, Gemini가 다음 태그로 감싼 응답을 냅니다:

```
<<<EDIT target="경로/파일.md" mode="section" section="## 서론">>>
(새로 쓴 내용 전체)
<<<END_EDIT>>>
```

응답 하단에 "📝 <파일>에 적용" 버튼이 자동 생성되며, 클릭 시 **diff 미리보기 모달**로 변경 사항을 확인한 뒤 승인하면 파일에 즉시 반영됩니다.

**모드**:
- `replace` — 파일 전체 교체
- `append` — 파일 끝에 추가
- `section` — 지정한 헤딩 섹션만 교체 (다음 동일·상위 레벨 헤딩 직전까지)

### 그래프 첨부

1. Obsidian 좌/우측에서 **그래프 뷰** 또는 **로컬 그래프** 패널을 엽니다
2. 채팅 헤더의 "🕸️ 그래프" 버튼 클릭 (또는 "그래프를 채팅에 첨부하고 열기" 명령)
3. "다음 전송에 첨부합니다" 알림 확인 후 질문 전송
4. Gemini 2.5 Flash가 이미지를 인식해 그래프 구조를 분석

### YouTube 자막 자동 수집

입력창에 YouTube URL(`https://www.youtube.com/watch?v=...` 또는 `https://youtu.be/...`)을 붙여넣으면 전송 직전에 자동으로 자막을 fetch하여 프롬프트에 포함합니다. API 키가 필요 없으며, 언어 우선순위는 Settings → 🎬 멀티미디어에서 조정(기본 `ko,en`).

### 시스템 프롬프트 규범

채팅은 다음 우선순위로 시스템 프롬프트를 구성합니다:

1. Settings → 💬 채팅 → **시스템 프롬프트 재정의**에 값이 있으면 그것만 사용
2. 없으면 기본 프롬프트 + `_memory/quality-guardrails.md`의 "작성 원칙" 섹션을 자동 결합

`_memory/quality-guardrails.md`를 편집하면 AI 응답 품질 규범이 즉시 바뀝니다. 근거 없는 수치·기관명·실적 생성을 금지하는 가드레일이 기본으로 포함되어 있습니다.

---

## v1.2 — MCP 서버 연결 (선택)

외부 도구(Slack 라우팅, 캘린더 조회 등)를 채팅에서 호출하려면 **HTTP/JSON-RPC 2.0 전송** MCP 서버를 등록합니다. (stdio 전송은 미지원)

### 등록 절차

1. Settings → 🔌 **MCP 서버** → JSON textarea에 서버 목록 입력:
   ```json
   [
     {
       "name": "slack",
       "url": "https://mcp.example.com/slack",
       "enabled": true,
       "authHeader": "Bearer xxxxx"
     }
   ]
   ```
2. **"도구 조회"** 버튼 클릭 → 개발자 콘솔(`Ctrl+Shift+I`)에 `[bid-intelligence MCP tools] [...]` 출력 확인
3. 채팅에서 자연어로 호출 — 예: "슬랙 #general 채널에 오늘 마감 공고 3건을 요약해서 보내줘"

### 동작 방식

MCP 도구가 1개 이상 등록되면 채팅은 **function calling 루프**로 전환됩니다 (스트리밍 대신 비스트리밍). Gemini가 도구 호출을 결정하면 자동으로 MCP 서버에 요청을 보내고, 응답을 다음 턴에 주입해 최대 5회까지 반복합니다.

도구 호출 시 채팅 메시지에 `🔧 도구 호출: \`이름\` {args}` 라인이 인라인으로 표시됩니다.

## 트러블슈팅

| 증상 | 원인 | 해결 |
|---|---|---|
| "📁 프로젝트 경로" 섹션 안 보임 | 플러그인 구버전 로드됨 | `Ctrl+P` → "Reload app without saving" |
| 사이드바 그대로 "0개" | 뷰 미갱신 | "다시 그리기" 재클릭 또는 플러그인 OFF/ON |
| 경로 맞는데 0개 | 폴더 미존재 또는 경로 오타 | 왼쪽 탐색기에서 실제 폴더 확인 |
| 채팅에서 "Gemini API 키가 설정되지 않았습니다" | 설정에 키 미입력 또는 오타 | Settings → 🤖 AI → 키 재입력 후 "테스트" |
| 채팅이 근거를 안 가져옴 | RAG 비활성 또는 벡터 DB 없음 | Settings → 🔍 RAG → 활성화 + "재인덱싱" |
| 재인덱싱이 중간에 멈춤 | API 무료 등급 RPM 초과 | 잠시 후 다시 실행(저장된 벡터는 유지 안 됨, 전체 재시도) |
| "📝 적용" 버튼이 안 뜸 | AI 응답에 EDIT 태그 없음 | 질문을 더 명시적으로 ("이 섹션을 … 로 다시 써줘") |
| 그래프 캡처 실패 | 그래프 뷰 미활성 | 그래프 패널을 먼저 열고 버튼 클릭 |
| YouTube 자막 수집 실패 | 영상에 자막 없음 또는 비공개 | 다른 영상으로 확인 |
| MCP "도구 조회"에서 오류 | HTTP 서버 아닌 stdio 서버 등 | HTTP/SSE 엔드포인트만 지원 — URL 재확인 |

## 개발자용 메모

### 저장 위치
- 설정값: `{vault}/.obsidian/plugins/bid-intelligence/data.json`
- 벡터 DB: `{vault}/.obsidian/plugins/bid-intelligence/vectors.json` (v1.2)

### 설정 필드 (v1.2 전체)
`contextFolder`, `analysisFolder`, `geminiApiKey`, `geminiModel`, `autoAnalyzeContext`, `autoFrontmatter`, `briefingKeywords`, `briefingAgencies`, `language`, `chatHistoryLimit`, `systemPromptOverride`, `ragEnabled`, `ragTopK`, `embeddingModel`, `youtubeCaptionLang`, `mcpServers[]`

### 설정 변경 시 영향 범위
- `contextFolder` / `analysisFolder`: ContextManagerView, BriefingDashboardView, AnalysisReportView, `analyzeAllContext()`, `shouldAutoAnalyze()`, `shouldIndex()` (v1.2 RAG 증분 인덱싱 판단)
- `geminiApiKey` 변경 시 Gemini 클라이언트 + Embeddings 클라이언트 동시 재초기화
- `mcpServers` 변경 시 `McpRegistry` 재구성

### 주요 유틸 파일 (v1.2)
- `src/utils/gemini.ts` — `generate` / `generateStream` (SSE) / `generateWithTools` (function calling)
- `src/utils/embeddings.ts` — text-embedding-004 래퍼
- `src/utils/chunker.ts` — 마크다운 청크 분할
- `src/utils/vectorStore.ts` — JSON 벡터 저장소
- `src/utils/contextBuilder.ts` — 핀/RAG 컨텍스트 조립
- `src/utils/youtube.ts` — 자막 fetch
- `src/utils/graphCapture.ts` — 그래프 canvas → base64
- `src/utils/mcpClient.ts` — MCP JSON-RPC 2.0 (HTTP)
- `src/utils/diffView.ts` — LCS 라인 diff
- `src/utils/guardrails.ts` — `_memory/quality-guardrails.md` 로드
- `src/views/ChatView.ts` — 채팅 허브
- `src/views/ApplyEditModal.ts` — 편집 제안 승인 UI

### 빌드·배포
```bash
cd d:/00work/260412-bid-intelligence-obsidian
npm run build
cp main.js "{vault}/.obsidian/plugins/bid-intelligence/main.js"
```

## 연관 문서

- [README.md](README.md) — 기능 개요
- [INSTALL.md](INSTALL.md) — 설치 가이드
- [_memory/quality-guardrails.md](_memory/quality-guardrails.md) — AI 응답 규범 (자동 주입)
- [_memory/README.md](_memory/README.md) — `_memory/` 폴더 운영 개요

## 배포 로그

- 2026-04-13: v1.1 (경로 설정 기능 최초 릴리스)
- 2026-04-14: v1.2 (수주 AI 채팅 + RAG + Apply Edit + 멀티미디어 + MCP + _memory 가드레일)
